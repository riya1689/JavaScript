// Creating an array of books containing different book.
const books =['math', 'computer', 'chemistry', 'science', 'physics'];

// Using the includes method to check if the array contains a javascript book.
console.log(books.includes('Javascript'));
