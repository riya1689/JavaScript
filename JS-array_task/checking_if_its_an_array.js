// Creating different variables, each containing either an array or a non-array value.
const empty =[];
const num =2;
const find = false;
const destination =['sajek', 'coxs bazar', 'sundarban'];
// Now using isArray to check if each variable is an array.

console.log(Array.isArray(find));

console.log(Array.isArray(empty));


